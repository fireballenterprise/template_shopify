# HowTo: Use Invoke Tasks

## Prerequisites
* [Local Setup](local_setup.md)

### Display Invoke Tasks
```bash
uv run --no-sync invoke -l
```

#### Autocorrect
```bash
uv run --no-sync invoke fix
```

#### Run All Tests
```bash
uv run --no-sync invoke test
```
