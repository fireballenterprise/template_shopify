---
name: setup
description: Use for initial project setup — creates the uv virtual environment and installs dependencies. Equivalent to /setup.
---

# Setup Workflow

Use this file as source of truth: `.github/prompts/setup.prompt.md`

When the user asks to set up the project for the first time, read that prompt file and follow it.

```bash
./setup.sh
```
